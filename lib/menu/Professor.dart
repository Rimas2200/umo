import 'package:flutter/material.dart';

class Professor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Экран преподавателей',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}