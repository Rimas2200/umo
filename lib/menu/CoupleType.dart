import 'package:flutter/material.dart';

class CoupleType extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Тип пар',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}