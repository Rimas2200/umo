import 'package:flutter/material.dart';

class Departament extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Кафедра',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}